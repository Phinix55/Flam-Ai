{# A3 corrected analysis. ZERO numeric literals. #}
# Part A — Corrected analysis

*Generated by `make render` from `{{ sources | join(", ") }}`. Commit
`{{ provenance.git_sha }}`.*

{{ n_records }} parallel sentences, {{ n_tokenizers }} tokenizers,
{{ n_denominators }} denominators, ratios normalised to
`{{ pivot }}`. Intervals are {{ provenance.confidence }} paired bootstrap over
sentence indices, {{ provenance.bootstrap_iterations }} resamples, seed
`{{ provenance.seed }}`.

{% for denominator in denominators %}
### Tokens per {{ denominator }}, relative to {{ pivot }}

{{ analysis_tables[denominator] }}

{% endfor %}
## Which denominator should drive the routing decision

TODO(pratik): interpretation — what each denominator holds constant, which one
holds *meaning* constant, and which are diagnostics rather than decision
metrics. Say it with the numbers above.
