{# Part C decision memo. ZERO numeric literals -- the envelope is rendered
   from results/partc.json. Every judgment sentence is reserved: choosing a
   path, setting a threshold and naming a kill criterion are decisions, and
   CLAUDE.md rule 6 puts them with the human. #}
# Part C — Decision memo

*Envelope generated by `make partc` from `{{ sources | join(", ") }}`.
Commit `{{ provenance.git_sha }}`.*

Paths on the table: **(a)** SFT on synthetic "casualized" pairs, **(b)** a
small inference-time rewriter after the main model, **(c)** prompt engineering
only.

## Assumptions

Stated constraints: {{ partc.gpu_count }}× A100-80GB for {{ partc.project_weeks }} weeks;
{{ partc.reviewer_count }} native-speaker reviewer covering
{{ partc.covered_languages }} of {{ partc.target_languages }} target languages
at {{ partc.reviewer_hours_per_week }} h/week; review launches in
{{ partc.review_launch_weeks }} weeks; no external API budget.

Estimates I am supplying, each challengeable: reviewer throughput
{{ partc.items_per_hour }} items/h; {{ partc.sft_pairs }} SFT pairs;
{{ partc.sentences_per_response }} sentences per response;
{{ partc.epochs }} epochs; LoRA throughput between
{{ partc.tps_low }} and {{ partc.tps_high }} tokens/s.

**No external API budget** means synthetic pairs cannot come from a frontier
API; they must be generated by our own model on the same A100 that trains it.

## Arithmetic

### Human evaluation capacity

| quantity | value |
|---|---|
| reviewer-hours over the project | {{ partc.reviewer_hours }} |
| items at the stated rate | {{ partc.review_items }} |
| items per covered language | {{ partc.items_per_covered }} |
| items per language if split evenly across all targets | {{ partc.items_per_language_even }} |
| languages with zero native-speaker coverage | {{ partc.uncovered_count }} |

Uncovered: {{ partc.uncovered | join(", ") }}.

### Training cost

| quantity | value |
|---|---|
| measured tokens per response (Indic mean, Indic-aware tokenizer) | {{ partc.tokens_per_response }} |
| training tokens ({{ partc.sft_pairs }} pairs × {{ partc.epochs }} epochs) | {{ partc.training_tokens }} |
| GPU-hours at the higher throughput estimate | {{ partc.gpu_hours_high }} |
| GPU-hours at the lower throughput estimate | {{ partc.gpu_hours_low }} |
| GPU-hours available | {{ partc.gpu_hours_available }} |
| utilisation at the lower estimate | {{ partc.utilisation }} |

Sensitivity — GPU-hours at the lower throughput estimate, by dataset size:
{% for pairs, hours in partc.sensitivity %}{{ pairs }} pairs → {{ hours }} h{% if not loop.last %}; {% endif %}{% endfor %}.

### Serving cost of path (b)

Measured output goodput is {{ partc.goodput }} tok/s (B3, batch
{{ knee_batch_size }}). A rewriter regenerates the whole response, so it adds a
second pass over the same output-token count: {{ partc.goodput_halved }} tok/s
if the rewriter costs the same per token, on the same hardware and batch.

## Recommendation

TODO(pratik): interpretation — which path, and why.

## Success metric

TODO(pratik): interpretation — a metric with a numeric threshold, measurable
within the reviewer capacity computed above.

## Kill criterion

TODO(pratik): interpretation — the observation that would abandon the chosen
path, and by when. Dates available from the envelope: day one
{{ partc.day_one }}, GPU budget exhausted {{ partc.gpu_exhausted }}, decision
deadline {{ partc.decision_deadline }}, review launch {{ partc.review_launch }}.

## Day-1 experiment

TODO(pratik): interpretation — the first experiment, and what it would decide.

## The constraint that decides this

TODO(pratik): interpretation — read the arithmetic above and say which term is
actually binding.
